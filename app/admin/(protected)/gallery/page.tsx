import { SectionPlaceholder } from "@/components/admin/section-placeholder";

export default function AdminGalleryPage() {
  return <SectionPlaceholder description="Gallery albums are modelled in Prisma and seeded, but the media UI remains for a later phase." title="Gallery" />;
}
